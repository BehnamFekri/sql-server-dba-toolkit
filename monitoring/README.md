# Monitoring

Reusable SQL Server health checks for CPU, memory, storage, sessions, waits, databases and operational status.

Queries in this directory should be designed to be read-only and safe for production diagnostics.